name: DevOps Flask activity - Deploy (Optional)

on:
  # Only runs when you manually click "Run workflow" in GitHub.
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Placeholder deploy step
        run: |
          echo "This is an optional deploy workflow."
          echo "To deploy for real, configure AWS/EB and replace this step with EB CLI commands."